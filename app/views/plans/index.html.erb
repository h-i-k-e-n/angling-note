 <%= render "shared/plan_header" %>
    <div class="plans-contents">
         <div class="plans-main">
             <div class="contents row">
                <% @plans.each do |plan| %>
                <%= render partial: "plan", locals: { plan: plan } %>
                
                <% end %>
             </div>
         </div>
     </div>

<%= render "shared/footer_plan" %>